# CHANGELOG

## 2026-06-24
- 按资源发布工作台原型重构控制台信息架构：侧栏入口改成“总览 / 资源发布 / 访问凭据 / 调用观测”，并补充任务导向说明与导航 KPI。
- 把上游页改造成工作台：新增首屏任务卡、当前建议先处理、异常雷达、资源目录与已发布列表的运营摘要。
- 已发布列表增加筛选（全部 / 仅异常 / 仅自定义 MCP / 仅停用），并把“添加自定义 MCP”收进已发布运营区。
- 保留原有 provider / token / log API，不改后端协议，确保可直接重新构建并打包 LPK。
- 依据高保真原型把控制台正式落地到 lazycat-mcp Web UI，并准备按当天版本号 `2026.06.24` 重新构建 LPK 包。
- 安装后首屏改成更像产品工作台：首页新增统一入口 Hero、标准动作路径、连接中心与下一步指引，并把应用空状态从“纯文本占位”改成可执行引导。
- 2026-06-24：将控制台前端从 Go 内嵌单 HTML/inline JS 迁移为 Vue 3 + Vite 前端工程，保留现有 Go API 与嵌入发布方式；工作台首页、资源发布、访问凭据、调用观测改为组件化页面，并把前端构建接入 build.sh 与 Go embed。
- 2026-06-24：继续把 Vue 工作台收紧为更强产品态：新增跨页面操作顶栏（当前操作面 / 统一入口 / 运行信号 + 主操作按钮），并把整体视觉语言从黄绿色后台感收敛到更像正式工作台产品的冷色深色系。
- 2026-06-24：退役旧的 `internal/web/console.html` 与 `internal/web/console.css` 运行入口，正式切到 `frontend -> internal/web/dist -> web.go embed` 的 framework-only 前端链路。
- 2026-06-24：修复 framework 控制台安装后打不开的问题：`internal/app/server.go` 之前仍只放行 `/` 和旧 `/console.css`，导致 `/assets/*` 被 404；现已放行 SPA 静态资源并补充回归测试。
- 2026-06-24：继续修复 framework 控制台首页打不开：根路径被重写到 `/index.html` 后又被 `http.FileServer` 301 到 `./`，形成重定向循环；现改为直接回送嵌入的 `index.html`，避免安装后首页无限跳转。
- 2026-06-24：继续收紧安装后首页的信息架构：压缩左侧导航、去掉首屏重复入口信息，把首页收敛成单一主任务 + 单一接入模块，减少“后台字段页”观感。
- 2026-06-24：把首页主 CTA 改成按真实状态动态切换：异常入口优先于待发布，其次才是首个 Token 与稳定运营；并让首页主按钮、下一步文案、次级操作共用同一套状态机。
- 2026-06-24：继续把资源发布页首接入首页同一套状态机：异常态默认聚焦已发布列表 + 异常过滤，待发布态默认聚焦资源目录，让资源发布页不再只是固定说明卡。
- 2026-06-24：继续把访问凭据 / 调用观测页首并入统一状态机，并进一步压缩左侧导航权重，让二级工作面从“后台区块”更接近真正的产品工作台。
- 2026-06-24：做最后一轮总收口：统一四个主页面顶部节奏，进一步压缩总览信息密度，并把已发布列表 / Token 列表 / 日志列表统一成更像产品工作台的列表卡片层级。
- 2026-06-24：继续收掉过多解释文案与说明卡，把首页和三大工作面压缩成更纯净的工作台界面；保留状态机与运营结构，但减少后台式描述堆叠。
- 2026-06-24：继续压缩状态文案，去掉“先回修异常入口，再继续扩散使用”这类叙述型废话；首页与三大工作面只保留标题、状态、数字与动作。
- 2026-06-24：继续按正式产品界面重做整体层级：首页改成更纯净的 command center + 指标板，资源发布页改成更克制的工作台骨架，并同步收紧共享头部与样式系统。
- 2026-06-24：全部删除重写前端。建立严格产品信息架构，每页只做一件事：总览（系统健康+快捷动作+接入信息）、资源发布（目录+详情，不再混入异常处理）、访问凭据（创建+管理）、调用观测（日志+异常入口）。删掉所有数据重复、删掉导航中的 KPI 徽标、删掉多重堆叠的 hero 卡和异常面板。采用 Linear 风格克制深色工作台。
- 2026-06-24：修复 Skill-only 资源被误判为 MCP 异常的问题。后端对仅含 SKILL.md 且无 mcp-providers 的应用不再做 streamable_http 探测，避免出现 `unexpected content type: text/html` 误报；前端资源发布页对 Skill-only 应用隐藏 MCP 发布按钮，并在已发布列表标注为 `Skill 资源`。
- 2026-06-24：新增三态主题切换（白天 / 夜晚 / 系统）。前端增加主题开关与本地持久化（`lazycat-mcp-theme`），并监听系统 `prefers-color-scheme`，系统模式下随 OS 深浅色自动切换。
- 2026-06-24：将主题切换从侧栏底部上移到品牌区下方，提升可见性；底部仅保留语言与刷新操作。
- 2026-06-24：调整“资源发布”整体布局：新增顶部 KPI 行（应用数/待发布/已发布入口），左侧目录列改为更窄并支持吸顶，目录列表高度自适应视口，右侧详情卡片层级与间距统一，移动端自动单列。
- 2026-06-24：主题切换按钮由文字改为图标（☀ / 🌙 / ◐），并补充 `title/aria-label` 以保留可读性与可访问性。
- 2026-06-24：将“资源目录 / 已发布”切换从左栏底部上移到资源发布页顶部（KPI 下方），避免视线下沉；左栏只保留检索与应用列表。
- 2026-06-24：重构资源发布页信息架构，移除“资源目录/已发布”双标签。改为单路径：左侧“全部应用”选中对象，右侧同屏展示“当前应用可发布资源 + 当前应用已上线入口 + 全局入口运营”，降低概念切换成本。
- 2026-06-24：资源发布页文案与动作继续收敛：将“全局入口运营”改为“全部入口管理”；当当前应用已全部发布时，顶部不再显示“发布”按钮。
- 2026-06-24：根据反馈去掉“全局入口运营”语义，入口区标题进一步收敛为“入口管理”。
- 2026-06-24：按“能删就删”原则进一步瘦身全局 UI：去掉侧栏重复状态块、去掉资源发布页 KPI 与轨道筛选、去掉应用详情中的部署/公开路径冗余信息，仅保留应用列表 + 发布动作 + 入口管理核心路径。
- 2026-06-24：按反馈删除总览页三张“步骤卡片”（发布资源/发放凭据/调用观测），避免固定编号被误解为实时数据，首页仅保留状态与核心信息。
- 2026-06-24：主题切换进一步简化为单图标按钮（当前模式即当前图标），点击循环切换 白天→夜晚→系统，去掉三按钮并排显示。
- 2026-06-24：资源发布页继续收敛为“管理页”形态，删除右侧“当前应用可发布资源/当前应用已上线入口”两段，仅保留应用选择 + 顶部发布动作 + 入口管理主区，避免页面四不像。
- 2026-06-24：主题切换图标移动到品牌行（“M 懒猫 MCP”同一行）并重绘为更精致圆形图标按钮（☀︎ / ☾ / 🖥），保留单击循环与可访问标签。
- 2026-06-24：按反馈将主题图标替换为 ☀️ / 🌙 / 💻 三态，并保持单击循环切换。
- 2026-06-24：修复侧栏导航视觉未分隔问题，恢复导航按钮与底部语言/刷新按钮的独立样式（hover/active 高亮与间距）。
- 2026-06-24：资源管理页改为单列表模式（不再左右分栏），统一支持查询、状态筛选、分页、发布、上线/下线、删除与详情，收敛为一个 CRUD 管理页。
- 2026-06-24：资源列表页“详情”从原始 JSON alert 改为结构化详情弹窗（基础信息卡片化展示 + 路径可读）。
- 2026-06-24：列表操作按钮做视觉分层：发布（主色）、上线/下线（绿色）、详情（信息色）、删除（红色）。
- 2026-06-24：点击列表“发布”改为先弹参数表单（名称/slug/MCP 路径/传输方式）再确认发布，替代一键直发。
- 2026-06-25：资源管理列表操作规则调整：已发布条目仅保留“下线”，下线后不再显示“上线”，改为“重新发布”（走发布参数弹窗后确认）。
- 2026-06-25：重绘“入口详情”弹窗视觉层级：标题区增加状态徽标、字段卡片统一样式与更清晰的信息分组。
- 2026-06-25：资源列表“删除”操作新增二次确认弹窗，避免误删。
- 2026-06-25：删除确认由浏览器原生 confirm 改为应用内统一弹窗（含入口信息、取消/确定删除按钮），避免系统弹窗风格突兀。
- 2026-06-25：访问凭据页支持 Token 多次复制：创建时保存明文到本地（localStorage）并在已发放列表提供“复制 Token”按钮；删除 Token 时自动清理本地副本。
- 2026-06-25：所有删除类操作统一走应用内确认弹窗（入口删除 / Token 删除 / 日志清空），并在弹窗中增加字段说明（字段名 + 字段值）后再确认执行。
- 2026-06-25：资源管理列表新增“打开”按钮。对 `cloud.lazycat.app.lazycat-filedrop-skill` 与 `cloud.lazycat.app.lazycat-agent-browser-skill` 按规则拼接外部地址；三级域名不再硬编码，从懒猫应用返回的 `domain/subdomain` 自动提取（如 canway / nasw）。
- 2026-06-25：修复中英文切换下侧栏菜单不跟随语言的问题，导航文案改为双语字段并通过 `t(...)` 动态渲染（Overview / Management / Access tokens / Observability）。
